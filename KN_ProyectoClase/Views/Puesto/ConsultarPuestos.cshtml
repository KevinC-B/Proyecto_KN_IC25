@model List<KN_ProyectoClase.BaseDatos.ConsultarPuestos_Result>

@{
    ViewBag.Title = "ConsultarPuestos";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container-fluid">

    <div class="card shadow mb-4">
        <div class="card-header py-3">
            <h5 class="m-0 font-weight-bold text-primary">Mantenimiento de Puestos</h5>
        </div>
        <div class="card-body">
            <div id="dataTable_wrapper" class="dataTables_wrapper dt-bootstrap4">
                <div class="row">
                    <div class="col-sm-12 col-md-6"></div>
                </div>
                <div class="row">
                    <div class="col-sm-12">
                        <div class="margen">
                            <a class="btn btn-primary" href="@Url.Action("AgregarPuesto", "Puesto")">Agregar</a>
                        </div>
                        <table id="example" class="table table-bordered table-hover" width="100%" cellspacing="0" role="grid" aria-describedby="dataTable_info" style="width: 100%;">
                            <thead>
                                <tr>
                                    <th>#</th>
                                    <th>Nombre</th>
                                    <th>Descripción</th>
                                    <th>Acciones</th>
                                </tr>
                            </thead>
                            <tbody>

                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <td>@item.Id</td>
                                        <td>@item.Nombre</td>
                                        <td>@item.Descripcion</td>
                                        <td><a href="@Url.Action("ActualizarPuesto", "Puesto", new { q = item.Id})"><i class="fa fa-edit"></i></a></td>
                                    </tr>
                                }

                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<script src="~/Template/js/jquery.min.js"></script>
<script>

    $(document).ready(function () {
        var table = new DataTable('#example', {
            language: {
                url: '//cdn.datatables.net/plug-ins/2.2.2/i18n/es-ES.json',
            },
        });
    });

</script>